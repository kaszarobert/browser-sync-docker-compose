Proxy an existing website on localhost with Browser-Sync using docker. Works on Linux only.
The example docker-compose.yml is set to proxy the "http://localhost" and watch css files in a css/ folder.
